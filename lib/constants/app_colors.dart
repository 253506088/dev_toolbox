import 'package:flutter/material.dart';

class AppColors {
  // Primary: #0D9488
  static const Color primary = Color(0xFF0D9488);

  // Secondary: #14B8A6
  static const Color secondary = Color(0xFF14B8A6);

  // CTA: #F97316
  static const Color cta = Color(0xFFF97316);

  // Background: #F0FDFA
  static const Color background = Color(0xFFF0FDFA);

  // Text: #134E4A
  static const Color text = Color(0xFF134E4A);

  // Border: #134E4A (Same as text for high contrast)
  static const Color border = Color(0xFF134E4A);

  // Surface colors for cards/blocks
  static const Color surface = Colors.white;
}
